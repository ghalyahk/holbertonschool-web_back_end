This project demonstrates the basics of asynchronous programming in Python using async and await.

We create an asynchronous coroutine wait_random that:

Takes an integer argument max_delay (default = 10)

Waits for a random delay between 0 and max_delay

Returns the delay as a float
