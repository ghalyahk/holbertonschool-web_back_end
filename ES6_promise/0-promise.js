export default function getResponseFromAPI() {
  // Return a Promise that resolves successfully
  return new Promise((resolve, reject) => {
    resolve('Success'); // Example: resolve with a success message
  });
}
